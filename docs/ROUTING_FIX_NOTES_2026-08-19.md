Production navigation fix: a valid route could be drawn and ETA/distance populated, but a later noncritical camera-fit error propagated into the navigation error handler, causing the HUD to incorrectly say no drivable route. The routing request and route-data validation are now isolated from camera animation. A successful route remains successful even if fitBounds/easeTo is skipped. Cache version bumped so mobile clients load the corrected active runtime.