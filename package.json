{
  "name": "roviq-local",
  "version": "0.3.2",
  "private": true,
  "description": "ROVIQ Local — a global driver-curated, map-based local discovery guide.",
  "scripts": {
    "build": "node -e \"console.log('ROVIQ Local is a static Cloudflare Workers app; public/ is ready to deploy.')\"",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute roviq-local2 --file=./schema.sql",
    "db:seed": "wrangler d1 execute roviq-local2 --file=./seed.sql",
    "db:migrate:remote": "wrangler d1 execute roviq-local2 --remote --file=./schema.sql",
    "db:migrate:platform:remote": "wrangler d1 execute roviq-local2 --remote --file=./migrations/0004_moderation_network.sql && wrangler d1 execute roviq-local2 --remote --file=./migrations/0005_commercial_trust_layers.sql && wrangler d1 execute roviq-local2 --remote --file=./migrations/0006_taxonomy_and_advisories.sql && wrangler d1 execute roviq-local2 --remote --file=./migrations/0007_driver_trust_fields.sql",
    "db:seed:remote": "wrangler d1 execute roviq-local2 --remote --file=./seed.sql",
    "db:bootstrap:remote": "npm run db:migrate:remote && npm run db:migrate:platform:remote && npm run db:seed:remote"
  },
  "devDependencies": {
    "wrangler": "^4.68.1"
  }
}
