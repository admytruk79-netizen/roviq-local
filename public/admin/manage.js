(() => {
  'use strict';
  const $ = (s) => document.querySelector(s);
  let places = [], actor = null;
  const passKey='roviq_admin_passcode', tokenKey='roviq_curator_token';
  const credential=()=>sessionStorage.getItem(tokenKey)||sessionStorage.getItem(passKey)||'';
  const authHeaders=()=>{const v=credential();if(!v)return{};return v.startsWith('rql_')?{'Authorization':`Bearer ${v}`}:{'X-Admin-Passcode':v};};
  const headers=()=>({'Content-Type':'application/json',...authHeaders()});
  const esc=(v='')=>String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[c]));

  function storeCredential(v){sessionStorage.removeItem(passKey);sessionStorage.removeItem(tokenKey);if(v.startsWith('rql_'))sessionStorage.setItem(tokenKey,v);else sessionStorage.setItem(passKey,v);}
  function clearCredential(){sessionStorage.removeItem(passKey);sessionStorage.removeItem(tokenKey);}
  async function api(url,options={}){const res=await fetch(url,{...options,headers:{...headers(),...(options.headers||{})}});const data=await res.json().catch(()=>({success:false,error:'Invalid server response'}));if(res.status===401){clearCredential();showLogin('Credential expired or incorrect.');throw new Error('unauthorized');}if(!res.ok||!data.success){const e=new Error(data.error||'Request failed');e.data=data;throw e;}return data;}
  function showLogin(msg=''){ $('#login').hidden=false;$('#app').hidden=true;$('#loginMsg').textContent=msg; }
  function showApp(){ $('#login').hidden=true;$('#app').hidden=false;load(); }

  async function load(){const q=encodeURIComponent($('#search').value.trim()),s=encodeURIComponent($('#statusFilter').value);try{const data=await api(`/api/admin/places?q=${q}&status=${s}`);places=data.places||[];actor=data.actor||actor;renderActor();render();}catch(e){if(e.message!=='unauthorized')$('#list').innerHTML=`<div>${esc(e.message)}</div>`;}}
  function renderActor(){const el=$('#actorInfo');if(!el||!actor)return;const scope=actor.role==='super_admin'?'all markets':(actor.assignments||[]).map(a=>a.market_slug||a.city||a.region||a.country_code).filter(Boolean).join(', ')||'no assigned market';el.textContent=`Signed in: ${actor.display_name||actor.handle} · ${actor.role} · ${scope}`;$('#newBtn').hidden=actor.role!=='super_admin';}
  function render(){if(!places.length){$('#list').innerHTML='<div>No matching places in your assigned scope.</div>';return;}$('#list').innerHTML=places.map(p=>{const verified=p.verified_at?new Date(p.verified_at).toLocaleDateString():'never';return `<article class="place-row" data-id="${p.id}"><div><h3>${esc(p.name)}</h3><div class="meta">${esc(p.category)} · ${esc(p.address||p.city||'no address')}</div><div class="badges"><span class="badge">${esc(p.status)}</span><span class="badge">${esc(p.trust_level||(p.is_drivers_pick?'driver':'community'))}</span>${p.is_hidden?'<span class="badge warn">hidden</span>':''}${p.is_drivers_pick?'<span class="badge">★ driver pick</span>':''}${p.arrival_video_url?'<span class="badge">🎬 arrival video</span>':''}<span class="badge">verified ${esc(verified)}</span></div></div><div class="row-actions"><button class="btn btn-outline editBtn" type="button">Edit</button>${p.status==='pending'?'<button class="btn btn-fir approveBtn" type="button">Approve</button>':''}</div></article>`;}).join('');document.querySelectorAll('.editBtn').forEach(b=>b.onclick=()=>openEditor(Number(b.closest('.place-row').dataset.id)));document.querySelectorAll('.approveBtn').forEach(b=>b.onclick=()=>quickApprove(Number(b.closest('.place-row').dataset.id)));}
  async function quickApprove(id){try{await api('/api/admin/moderate',{method:'POST',body:JSON.stringify({place_id:id,action:'approve'})});await load();}catch(e){alert(e.message);}}
  function setVal(id,v){const el=$('#'+id);if(el.type==='checkbox')el.checked=Boolean(v);else el.value=v??'';}
  function openEditor(id=null){if(!id&&actor?.role!=='super_admin'){alert('Only a super-admin can create catalogue records directly. Use Suggest a Spot for normal submissions.');return;}const p=places.find(x=>x.id===id)||{};$('#editorTitle').textContent=id?`Edit ${p.name}`:'Add place';['placeId','name','category','description','address','lat','lng','photo_url','virtual_image_url','arrival_video_url','hours','status','trust_level','submitted_by','moderation_note','is_drivers_pick','is_hidden'].forEach(k=>setVal(k,k==='placeId'?(id||''):p[k]));if(!id){setVal('category','food');setVal('status','approved');setVal('trust_level','roviq');setVal('submitted_by',actor?.handle||'ROVIQ curator');}$('#verify_now').checked=!id;$('#hideBtn').hidden=!id;$('#editorMsg').textContent='';const canPick=['regional_admin','super_admin'].includes(actor?.role);$('#trust_level').disabled=!canPick;$('#is_drivers_pick').disabled=!canPick;$('#editorWrap').classList.add('open');}
  function closeEditor(){$('#editorWrap').classList.remove('open');}
  function payload(){const p={name:$('#name').value.trim(),category:$('#category').value,description:$('#description').value.trim(),address:$('#address').value.trim(),lat:Number($('#lat').value),lng:Number($('#lng').value),photo_url:$('#photo_url').value.trim(),virtual_image_url:$('#virtual_image_url').value.trim(),arrival_video_url:$('#arrival_video_url').value.trim(),hours:$('#hours').value.trim(),status:$('#status').value,submitted_by:$('#submitted_by').value.trim(),moderation_note:$('#moderation_note').value.trim(),is_hidden:$('#is_hidden').checked,verify_now:$('#verify_now').checked};if(!$('#trust_level').disabled)p.trust_level=$('#trust_level').value;if(!$('#is_drivers_pick').disabled)p.is_drivers_pick=$('#is_drivers_pick').checked;return p;}
  $('#editor').addEventListener('submit',async e=>{e.preventDefault();const id=$('#placeId').value,data=payload();$('#editorMsg').textContent='Saving…';try{if(id)await api(`/api/admin/places/${id}`,{method:'PATCH',body:JSON.stringify(data)});else await api('/api/admin/places',{method:'POST',body:JSON.stringify(data)});$('#editorMsg').textContent='Saved.';await load();setTimeout(closeEditor,350);}catch(e){if(e.data?.duplicates)$('#editorMsg').textContent=`Possible duplicate: ${e.data.duplicates.map(d=>d.name).join(', ')}.`;else $('#editorMsg').textContent=e.message;}});
  $('#hideBtn').onclick=async()=>{const id=$('#placeId').value;if(!id)return;if(!confirm('Hide this place from the public map?'))return;try{await api(`/api/admin/places/${id}`,{method:'DELETE'});await load();closeEditor();}catch(e){$('#editorMsg').textContent=e.message;}};
  $('#closeEditor').onclick=closeEditor;$('#editorWrap').addEventListener('click',e=>{if(e.target===$('#editorWrap'))closeEditor();});$('#newBtn').onclick=()=>openEditor();$('#refreshBtn').onclick=load;$('#statusFilter').onchange=load;let timer;$('#search').oninput=()=>{clearTimeout(timer);timer=setTimeout(load,250);};$('#loginBtn').onclick=async()=>{const v=$('#pass').value.trim();if(!v)return;storeCredential(v);try{const data=await api('/api/admin/places');actor=data.actor||null;showApp();}catch(e){if(e.message!=='unauthorized')showLogin(e.message);}};$('#pass').addEventListener('keydown',e=>{if(e.key==='Enter')$('#loginBtn').click();});$('#logoutBtn')?.addEventListener('click',()=>{clearCredential();actor=null;$('#pass').value='';showLogin();});
  if(credential())showApp();else showLogin();
})();