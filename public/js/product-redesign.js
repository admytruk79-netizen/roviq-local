(() => {
'use strict';

const $ = (s) => document.querySelector(s);
let map;
let markers = [];
let userMarker;
let places = [];
let userLocation = null;
let activeFilter = 'all';
let activeView = 'map';

const esc = (s) => String(s ?? '').replace(/[&<>"']/g, (c) => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const CATEGORY_ICON={food:'🍴',coffee:'☕',nature:'⌁',culture:'◫',breweries:'◉'};
const normalizeCategory=(v)=>String(v||'').trim().toLowerCase();
const isPick=(p)=>p?.trust_level==='roviq'||p?.trust_level==='driver'||Number(p?.is_drivers_pick)===1;
function trust(p){if(p.network_type)return['network','ROVIQ NETWORK'];if(p.trust_level==='roviq')return['roviq','ROVIQ PICK'];if(Number(p.is_drivers_pick)===1||p.trust_level==='driver')return['driver','DRIVER’S PICK'];return['standard','CURATED'];}
function why(p){return p.editorial_reason||p.why_stop||p.description||'A locally curated stop worth knowing about while you’re on the road.';}
function dist(p){if(!userLocation)return'';const lat=Number(p.lat),lng=Number(p.lng);if(!Number.isFinite(lat)||!Number.isFinite(lng))return'';const R=3958.8,rad=x=>x*Math.PI/180,dLat=rad(lat-userLocation.lat),dLng=rad(lng-userLocation.lng),a=Math.sin(dLat/2)**2+Math.cos(rad(userLocation.lat))*Math.cos(rad(lat))*Math.sin(dLng/2)**2;return`${(2*R*Math.asin(Math.sqrt(a))).toFixed(1)} mi`;}
function locationLabel(){const s=window.__ROVIQ_LOCATION_SCOPE||{},b=[s.city,s.region].filter(Boolean);return b.length?b.join(', '):(s.label&&s.label!=='Near me'?s.label:'Near you');}
function shortAddress(p){const s=String(p.locality||p.address||'').trim();return s?s.split(',').map(x=>x.trim()).filter(Boolean).slice(0,2).join(', '):'';}
const openLabel=p=>p.hours||'';
function applyAutomaticTheme(){const saved=localStorage.getItem('roviq_theme'),now=new Date(),hour=now.getHours(),mode=saved==='day'||saved==='night'?saved:(hour>=7&&hour<19?'day':'night');const wildcard=mode==='night'&&[5,6,0].includes(now.getDay());document.documentElement.dataset.theme=mode;document.body?.classList.toggle('theme-day',mode==='day');document.body?.classList.toggle('theme-night',mode==='night');document.body?.classList.toggle('theme-wildcard',wildcard);let badge=$('#wildcard-badge');if(!badge){const group=$('.brand-group')||$('.brandrow');if(group){badge=document.createElement('span');badge.id='wildcard-badge';badge.className='wildcard-badge';badge.textContent='◆ Weekend Special';group.appendChild(badge);}}if(badge)badge.hidden=!wildcard;}
function injectHeader(){const row=$('.brandrow');if(!row)return;if(!document.querySelector('.discovery-copy')){const c=document.createElement('div');c.className='discovery-copy';c.innerHTML='<h1>Worth stopping for near you</h1><div class="location-line"><strong id="dynamic-location">Finding your location…</strong><button id="redesign-change-location" type="button">Change location</button></div>';row.insertAdjacentElement('afterend',c);$('#redesign-change-location')?.addEventListener('click',()=>$('#change-location')?.click());}const a=$('.location-actions');if(a)a.style.display='none';const chips=$('#chips');if(chips)chips.innerHTML='<button class="chip active" data-category="all" type="button">All</button><button class="chip" data-category="picks" type="button">ROVIQ Picks</button><button class="chip" data-category="food" type="button">Food</button><button class="chip" data-category="coffee" type="button">Coffee</button><button class="chip" data-category="nature" type="button">Nature</button><button class="chip" data-category="culture" type="button">Culture</button><button class="chip" data-category="breweries" type="button">Breweries</button>';}
function injectNav(){if(document.querySelector('.bottom-nav'))return;const nav=document.createElement('nav');nav.className='bottom-nav';nav.setAttribute('aria-label','Primary');nav.innerHTML='<button class="active" data-dest="explore"><span>⌖</span>Explore</button><button data-dest="picks"><span>◆</span>Picks</button><button data-dest="saved"><span>☆</span>Saved</button><button data-dest="suggest"><span>＋</span>Suggest</button><button data-dest="profile"><span>○</span>Profile</button>';document.body.appendChild(nav);nav.addEventListener('click',e=>{const b=e.target.closest('button');if(!b)return;nav.querySelectorAll('button').forEach(x=>x.classList.remove('active'));b.classList.add('active');if(b.dataset.dest==='suggest'){ $('#open-suggest')?.click();return;}if(b.dataset.dest==='picks'){setFilter('picks');setView('list');return;}if(b.dataset.dest==='saved'){setView('list');renderList(places.filter(isSaved));return;}if(b.dataset.dest==='profile'){alert('Profile and contributor identity are prepared as a future account surface.');return;}setFilter('all');setView('map');});}
function mapFallback(){const el=$('#map');if(!el)return;el.innerHTML='<div class="map-fallback"><div class="map-fallback-card"><h2>Map is temporarily unavailable</h2><p>Your location still works. Browse nearby ROVIQ places in List view.</p><button id="fallback-list">Open List</button></div></div>';$('#fallback-list')?.addEventListener('click',()=>setView('list'));}
function driveHud(){const el=$('#map');if(!el||el.querySelector('.drive-hud'))return;el.classList.add('drive-map');const hud=document.createElement('div');hud.className='drive-hud';hud.innerHTML=`<div class="drive-hud__market">ROVIQ LOCAL<strong>${esc(locationLabel())}</strong></div><div class="drive-hud__count"><strong id="drive-hud-count">0</strong> discoveries</div>`;el.appendChild(hud);}
function updateDriveHud(){const count=$('#drive-hud-count');if(count)count.textContent=String(visiblePlaces().length);const market=document.querySelector('.drive-hud__market strong');if(market)market.textContent=locationLabel();}
function initLeaflet(){if(!window.L){mapFallback();return;}const el=$('#map');if(!el)return;el.innerHTML='';const s=window.__ROVIQ_LOCATION_SCOPE||{},center=Number.isFinite(+s.lat)&&Number.isFinite(+s.lng)?[+s.lat,+s.lng]:[45.5231,-122.6765];try{map=L.map(el,{zoomControl:false}).setView(center,12);window.__ROVIQ_MAP_INSTANCE=map;window.dispatchEvent(new CustomEvent('roviq:map-ready'));L.control.zoom({position:'bottomright'}).addTo(map);L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:19,attribution:'© OpenStreetMap contributors'}).addTo(map);driveHud();setTimeout(()=>map.invalidateSize(),120);}catch(err){console.error('Leaflet map initialization failed',err);mapFallback();}}
function markerIcon(type){return L.divIcon({className:'',html:`<div class="roviq-marker ${type}"></div>`,iconSize:[28,28],iconAnchor:[14,28]});}
function visiblePlaces(){if(activeFilter==='all')return places;if(activeFilter==='picks')return places.filter(isPick);return places.filter(p=>normalizeCategory(p.category)===activeFilter);}
function renderMarkers(items=visiblePlaces()){if(!map||!window.L)return;markers.forEach(m=>m.remove());markers=[];items.forEach(p=>{const lat=Number(p.lat),lng=Number(p.lng);if(!Number.isFinite(lat)||!Number.isFinite(lng))return;const[type]=trust(p);markers.push(L.marker([lat,lng],{icon:markerIcon(type)}).addTo(map).on('click',()=>preview(p)));});updateDriveHud();}
function preview(p){document.querySelector('.place-preview')?.remove();const[type,label]=trust(p),card=document.createElement('div');card.className='place-preview';card.innerHTML=`<span class="trust-pill ${type}">${label}</span><h3>${esc(p.name)}</h3><div class="meta">${esc(p.category||'Place')}${dist(p)?' · '+dist(p):''}${openLabel(p)?' · '+esc(openLabel(p)):''}</div><p><strong>Why stop here?</strong> ${esc(why(p))}</p><div class="preview-actions"><button data-save>${isSaved(p)?'Saved':'Save'}</button><button data-view>View place</button><button class="primary" data-dir>Directions</button></div>`;$('#map')?.appendChild(card);card.querySelector('[data-view]')?.addEventListener('click',()=>{setView('list');requestAnimationFrame(()=>[...document.querySelectorAll('.place-card')].find(x=>x.dataset.id===String(p.id))?.scrollIntoView({behavior:'smooth',block:'center'}));});card.querySelector('[data-dir]')?.addEventListener('click',()=>window.open(`https://www.google.com/maps/dir/?api=1&destination=${p.lat},${p.lng}`,'_blank'));card.querySelector('[data-save]')?.addEventListener('click',()=>savePlace(p,card.querySelector('[data-save]')));}
function savePlace(p,b){let saved;try{saved=new Set(JSON.parse(localStorage.getItem('roviq_saved')||'[]').map(String));}catch{saved=new Set();}const id=String(p.id);if(saved.has(id)){saved.delete(id);if(b)b.textContent='Save';}else{saved.add(id);if(b)b.textContent='Saved';}localStorage.setItem('roviq_saved',JSON.stringify([...saved]));}
function isSaved(p){try{return new Set(JSON.parse(localStorage.getItem('roviq_saved')||'[]').map(String)).has(String(p.id));}catch{return false;}}
function renderList(items=visiblePlaces()){const list=$('#list-view');if(!list)return;if(!items.length){const label=activeFilter==='all'?'nearby':activeFilter==='picks'?'ROVIQ Picks':activeFilter;list.innerHTML=`<div class="empty-state"><strong>No ${esc(label)} stops yet.</strong><span>Try another category or suggest a place worth knowing about.</span></div>`;return;}list.innerHTML=items.map(p=>{const[type,label]=trust(p),cat=normalizeCategory(p.category),photo=p.photo_url?`style="background-image:url('${esc(p.photo_url)}')"`:'',fallback=CATEGORY_ICON[cat]||'•',meta=[dist(p),openLabel(p),cat].filter(Boolean).join(' · '),addr=shortAddress(p);return`<article class="place-card" data-id="${esc(p.id)}"><div class="place-photo${p.photo_url?' has-photo':''}" ${photo}>${p.photo_url?'':esc(fallback)}</div><div class="place-info"><span class="trust-pill ${type}">${label}</span><div class="place-name">${esc(p.name)}</div><div class="why-stop">${esc(why(p))}</div><div class="place-meta">${esc(meta)}</div>${addr?`<span class="place-address" title="${esc(p.address||addr)}">${esc(addr)}</span>`:''}</div><div class="card-actions"><button data-save>${isSaved(p)?'Saved':'Save'}</button><button data-view>View place</button><button data-dir>Directions</button></div></article>`;}).join('');list.querySelectorAll('.place-card').forEach(card=>{const p=items.find(x=>String(x.id)===card.dataset.id);if(!p)return;const go=()=>{setView('map');preview(p);if(map)map.setView([Number(p.lat),Number(p.lng)],Math.max(map.getZoom(),14));};card.querySelector('[data-view]')?.addEventListener('click',go);card.querySelector('[data-dir]')?.addEventListener('click',()=>window.open(`https://www.google.com/maps/dir/?api=1&destination=${p.lat},${p.lng}`,'_blank'));card.querySelector('[data-save]')?.addEventListener('click',e=>{e.stopPropagation();savePlace(p,e.currentTarget);});card.addEventListener('click',e=>{if(!e.target.closest('button'))go();});});}
function renderCurrent(){renderMarkers();renderList();updateDriveHud();window.dispatchEvent(new CustomEvent('roviq:places-loaded',{detail:{count:places.length}}));}
async function fetchApproved(url){const r=await (window.__ROVIQ_ORIGINAL_FETCH||window.fetch)(url.toString(),{cache:'no-store'}),d=await r.json();return r.ok&&d?.success&&Array.isArray(d.places)?d.places:[];}
async function loadPlaces(){try{const url=new URL('/api/places',location.origin);url.searchParams.set('status','approved');let loaded=await fetchApproved(url);if(!loaded.length){const fallback=new URL('/api/places',location.origin);fallback.searchParams.set('status','approved');loaded=await fetchApproved(fallback);}places=loaded;window.__roviqPlaces=places;renderCurrent();}catch(err){console.error('Failed to load ROVIQ places',err);places=[];window.__roviqPlaces=[];renderCurrent();}}
function setFilter(f){activeFilter=f||'all';$('#chips')?.querySelectorAll('.chip').forEach(c=>c.classList.toggle('active',c.dataset.category===activeFilter));renderCurrent();}
function initFilters(){$('#chips')?.addEventListener('click',e=>{const c=e.target.closest('.chip');if(!c)return;e.preventDefault();setFilter(c.dataset.category);});}
function setView(v){activeView=v==='list'?'list':'map';$('#toggle-map')?.classList.toggle('active',activeView==='map');$('#toggle-list')?.classList.toggle('active',activeView==='list');const me=$('#map'),le=$('#list-view');if(me)me.style.display=activeView==='map'?'':'none';if(le)le.hidden=activeView!=='list';if(activeView==='list')renderList();else{document.querySelector('.place-preview')?.remove();setTimeout(()=>map?.invalidateSize(),50);}}
function initViewToggle(){$('#toggle-map')?.addEventListener('click',()=>setView('map'));$('#toggle-list')?.addEventListener('click',()=>setView('list'));}
function locationInit(){const label=$('#dynamic-location');if(label)label.textContent=locationLabel();const s=window.__ROVIQ_LOCATION_SCOPE||{};if(Number.isFinite(Number(s.lat))&&Number.isFinite(Number(s.lng))){userLocation={lat:Number(s.lat),lng:Number(s.lng)};if(map&&window.L){userMarker?.remove();userMarker=L.marker([userLocation.lat,userLocation.lng],{icon:L.divIcon({className:'',html:'<div class="user-dot"></div>',iconSize:[16,16],iconAnchor:[8,8]})}).addTo(map);}}updateDriveHud();loadPlaces();}
document.addEventListener('DOMContentLoaded',()=>{applyAutomaticTheme();setInterval(applyAutomaticTheme,5*60*1000);injectHeader();injectNav();initLeaflet();initViewToggle();initFilters();setFilter('all');setView('map');locationInit();});
})();