import {loadApprovedPlaces,toggleSaved,savedIds} from './services.js';import {DiscoveryEngine} from './discovery.js';
const $=s=>document.querySelector(s),icons={food:'🍴',coffee:'☕',breweries:'◉',nature:'✦',scenic:'✦',culture:'◫',markets:'▣',recreation:'⌁',family:'◇',lodging:'⌂',automotive:'◈',charging:'ϟ',services:'•',other:'◆'};let map,engine,catalog=[],markers=new Map(),userMarker,state='rest',selected=null;
function setState(s){state=s;document.body.dataset.state=s;engine?.setState(s);refresh();if(s==='wild')wild()}
function key(p){return String(p.id??`${p.lat},${p.lng}`)}function isPick(p){return p.trust_level==='roviq'||+p.is_drivers_pick===1}
function refresh(){if(!map||!engine)return;const wanted=new Map(engine.visible().map(p=>[key(p),p]));for(const [k,m] of markers)if(!wanted.has(k)){m.remove();markers.delete(k)}for(const [k,p] of wanted){if(markers.has(k))continue;const el=document.createElement('button');el.className='rq-marker'+(isPick(p)?' pick':'');el.innerHTML=`<span>${icons[String(p.category_key||p.category||'other').toLowerCase()]||'◆'}</span>`;el.onclick=e=>{e.stopPropagation();selected=p;document.querySelectorAll('.rq-marker.selected').forEach(x=>x.classList.remove('selected'));el.classList.add('selected');showCard(p);map.easeTo({center:[+p.lng,+p.lat],zoom:Math.max(map.getZoom(),13.7),pitch:28,duration:650,essential:true})};markers.set(k,new maplibregl.Marker({element:el,anchor:'center'}).setLngLat([+p.lng,+p.lat]).addTo(map))}}
function showCard(p){const c=$('#rq-card');c.hidden=false;$('#rq-kicker').textContent=isPick(p)?'◆ ROVIQ PICK':'CURATED';$('#rq-name').textContent=p.name||'Place';$('#rq-meta').textContent=p.category||'';$('#rq-desc').textContent=p.editorial_reason||p.why_stop||p.description||'Worth knowing about while you are on the road.';$('#rq-save').textContent=savedIds().has(String(p.id))?'Saved':'Save'}
function wild(){const pool=engine.visible();if(!pool.length)return;const p=pool[Math.floor(Math.random()*pool.length)];selected=p;showCard(p);$('#rq-kicker').textContent='WILD DISCOVERY';map.easeTo({center:[+p.lng,+p.lat],zoom:13.8,pitch:34,duration:850,essential:true})}
function locate(){navigator.geolocation?.getCurrentPosition(pos=>{const ll=[pos.coords.longitude,pos.coords.latitude];if(userMarker)userMarker.setLngLat(ll);else{const el=document.createElement('div');el.className='rq-user';userMarker=new maplibregl.Marker({element:el,anchor:'center'}).setLngLat(ll).addTo(map)}map.flyTo({center:ll,zoom:13.8,pitch:state==='rest'?16:28,speed:1.1,curve:1.2,essential:true})},()=>{}, {enableHighAccuracy:false,timeout:8000,maximumAge:300000})}
function savedView(){const ids=savedIds(),p=catalog.find(x=>ids.has(String(x.id)));if(p){setState('explore');selected=p;showCard(p);map.easeTo({center:[+p.lng,+p.lat],zoom:14,pitch:28,duration:650})}}
async function init(){map=new maplibregl.Map({container:'map',style:'https://tiles.stadiamaps.com/styles/alidade_smooth_dark.json',center:[-122.6765,45.5231],zoom:11.5,pitch:16,bearing:0,minZoom:3,maxZoom:18});engine=new DiscoveryEngine(map);map.on('load',async()=>{try{catalog=await loadApprovedPlaces()}catch(e){console.error(e);catalog=[]}engine.setCatalog(catalog);setState('rest');locate()});map.on('moveend',refresh);map.on('zoomend',refresh);map.on('dragstart',()=>{if(state==='rest')setState('explore')});map.on('click',()=>{if(state==='rest')setState('explore')});$('#rq-discover').onclick=()=>setState('explore');$('#rq-wild').onclick=()=>setState('wild');$('#rq-search').onclick=()=>setState('explore');$('#rq-locate').onclick=locate;$('#rq-home').onclick=()=>setState('rest');$('#rq-explore').onclick=()=>setState('explore');$('#rq-saved').onclick=savedView;$('#rq-save').onclick=()=>{if(selected)$('#rq-save').textContent=toggleSaved(selected.id)?'Saved':'Save'};$('#rq-go').onclick=()=>{if(!selected)return;window.open(`https://www.google.com/maps/dir/?api=1&destination=${selected.lat},${selected.lng}`,'_blank')}}
init().catch(console.error);