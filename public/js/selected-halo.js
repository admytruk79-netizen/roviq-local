(()=>{'use strict';function clear(){document.querySelectorAll('.roviq-gl-marker.selected').forEach(x=>x.classList.remove('selected'))}function addAt(){/* MapLibre marker selection is handled by the native ROVIQ controller. */}document.addEventListener('click',e=>{if(e.target.closest?.('.chip,.bottom-nav button[data-dest="explore"]'))clear()},true);window.ROVIQSelectedHalo={clear,addAt};})();